# =============================================================================
# Xenon Agent — 环境变量配置模板
# =============================================================================
# 复制此文件为 .env 并填入真实值：
#   cp .env.example .env          # Linux/macOS
#   copy .env.example .env        # Windows
#
# 不要将 .env 提交到版本控制，它已在 .gitignore 中。
# =============================================================================

# ─────────────────────────────────────────────────────────────────────────────
# 核心引擎 — 必需
# ─────────────────────────────────────────────────────────────────────────────
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ─────────────────────────────────────────────────────────────────────────────
# 视觉识别 — 可选（Vision）
# 支持: XENON_VISION_API_KEY / ZHIPUAI_API_KEY / BIGMODEL_API_KEY / GLM_API_KEY
# ─────────────────────────────────────────────────────────────────────────────
XENON_VISION_API_KEY=
# XENON_VISION_BASE_URL=https://api.openai.com/v1
# XENON_VISION_MODEL=gpt-4o

# ─────────────────────────────────────────────────────────────────────────────
# 语音识别 & 合成 — 可选（ASR / TTS）
# 使用阿里云通义千问 API（DashScope）
# ─────────────────────────────────────────────────────────────────────────────
DASHSCOPE_API_KEY=

# ─────────────────────────────────────────────────────────────────────────────
# WebUI 界面 — 可选
# ─────────────────────────────────────────────────────────────────────────────
XENON_WEB_APP_KEY=change_me_for_local_dev
# XENON_WEBUI_HOST=127.0.0.1
# XENON_WEBUI_PREWARM=auto
# XENON_WEBUI_FILE_WATCHER=auto

# ─────────────────────────────────────────────────────────────────────────────
# GitHub 集成 — 可选
# ─────────────────────────────────────────────────────────────────────────────
# GITHUB_TOKEN=your_github_personal_access_token
# GH_TOKEN=your_github_personal_access_token
