# 安全策略

## 支持的版本

| 版本 | 状态 |
|------|------|
| ≥ 1.0.0 | ✅ 活跃维护 |

## 报告安全漏洞

Xenon 是一个涉及终端执行、SSH 远程连接、文件系统编辑等**高权限操作**的 AI 智能体系统。如果你发现了安全漏洞，请**不要**公开提交 Issue。

请通过以下方式报告：

1. **私有方式**：联系项目维护者 [photowork](https://github.com/photowork)
2. **加密通信**：如有必要，可先通过 GitHub Issue 请求安全联系方式

我们承诺在 **48 小时内**确认收到报告，并在**合理时间内**修复并发布公告。

---

## 安全最佳实践

### 1. 环境变量与密钥管理

- **永远不要**将真实的 API 密钥、Token 或密码提交到 Git 仓库
- 使用 `.env` 文件（已提供 `.env.example` 作为模板）存储敏感配置
- 将 `.env` 添加到 `.gitignore` 中（默认已配置）
- 定期轮换密钥

### 2. 高权限工具的安全使用

Xenon 内置了以下高权限工具，使用时需注意：

| 工具模块 | 风险等级 | 说明 |
|----------|---------|------|
| `terminal_handler` | 🔴 高 | 可执行任意系统命令 |
| `ssh_handler` | 🔴 高 | 可连接远程服务器 |
| `code_editor_handler` | 🟡 中 | 可读写任意文件 |
| `file_manager` | 🟡 中 | 文件系统操作 |
| `github_manager` | 🟡 中 | 可推送代码到仓库 |

**建议：**
- 不要在不受信任的网络环境中启用 SSH 模块
- 限制终端命令的执行范围（可通过 `configs/` 自定义白名单）
- 定期审计工具调用日志

### 3. 部署安全

- **生产环境**：建议在隔离的容器或虚拟机中运行 Xenon
- **WebUI 访问**：默认监听 `127.0.0.1`，如需公网访问请配置反向代理（如 Nginx）并启用 HTTPS
- **WebUI 密钥**：设置 `XENON_WEB_APP_KEY` 环境变量，防止未授权访问

### 4. 依赖安全

- 定期运行 `pip audit` 或 `safety check` 检查依赖漏洞
- 保持 Python 和相关依赖更新到最新稳定版
- 不要以 root/管理员权限运行 Xenon

---

## 安全配置检查清单

- [ ] `.env` 已加入 `.gitignore`
- [ ] `DEEPSEEK_API_KEY` 等密钥已配置
- [ ] WebUI 密钥 `XENON_WEB_APP_KEY` 已设置（非默认值）
- [ ] SSH 模块仅在需要时加载
- [ ] 生产环境使用非 root 用户运行
- [ ] 已配置日志轮转，防止日志泄露

---

## 相关文档

- [环境变量配置](./.env.example)
- [快速开始指南](./README.md#快速开始)
